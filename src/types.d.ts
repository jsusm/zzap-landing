export type itemGrid = {
  title: string;
  content: string;
  iconName: string;
};

export type AccordionItem = {
  title: string;
  desc: string;
};

export type Link = {
  text: string;
  href: string;
};
